#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	cargo build --release

override_dh_auto_install:
	mkdir -p debian/crc32-v2/usr/lib
	cp target/release/libcrc32_v2.so debian/crc32-v2/usr/lib/ 2>/dev/null || true

override_dh_auto_test:
	cargo test
